# Trino connection — local Docker preset
# Use this for testing against ./docker/docker-compose.yml
#
#   cp templates/trino.env.docker ~/.trino.env

TRINO_HOST=localhost
TRINO_PORT=8080
TRINO_USER=admin
TRINO_PASSWORD=
TRINO_HTTPS=false
TRINO_VERIFY_SSL=false
TRINO_CATALOG=tpch
TRINO_SCHEMA=tiny
